{
  "id": "esl-crossbar-array",
  "name": "ESL analog crossbar row/col array (parametric \\foreach generator)",
  "type": "template",
  "domain": ["circuits", "esl-architecture"],
  "tags": ["circuit-schematic", "crossbar", "analog", "in-memory-compute", "weight-cell", "vmm", "foreach", "generator", "array-scale", "row-col-indexed"],
  "venue": ["ISSCC", "VLSI Symposium", "TCAS-I", "JSSC"],
  "author": "cp-4892 (WP-4, ADR-0005 D5) -- extends the esl-crossbar-kcl PoC (cp-4856) with a \\foreach-driven array generator",
  "license": "CC0-1.0",
  "requires": ["xcolor", "tikz", "calc", "arrows.meta"],
  "preview": "preview.svg",
  "description": "Parametric nrows x ncols VMM crossbar-cell array (contract §1a `crossbar-array`), generated by nested \\foreach loops with row/col-indexed node names, row voltage sources, and per-column KCL summation nodes. Wiring uses O(1)-per-row/column rails (a single colinear draw through every cell in the line), not O(rows*cols) point-to-point wires, so the array stays both correct-looking and legible at scale. Proven at 64x64 with celllabelmode=none (blank cell fill) and edgelabelstride (only every Nth row/col index annotated) as the explicit legibility rules. Colors reuse the esl-crossbar-kcl domain-semantic scheme (blue=input/activation, green=analog weight cell, orange=analog current/summation).",
  "edit_contract": {
    "parameters": [
      {"name": "\\nrows", "type": "int", "meaning": "number of crossbar rows (VMM input rows)", "default": "2"},
      {"name": "\\ncols", "type": "int", "meaning": "number of crossbar columns (VMM output columns)", "default": "2"},
      {"name": "\\rowgap", "type": "length-cm", "meaning": "vertical gap between rows", "default": "0.7"},
      {"name": "\\colgap", "type": "length-cm", "meaning": "horizontal gap between columns", "default": "0.7"},
      {"name": "\\cellsize", "type": "length-cm", "meaning": "crossbar-cell / vsource / kclnode node size", "default": "0.5"},
      {"name": "\\celllabelmode", "type": "enum(full,none)", "meaning": "\"full\" prints $G_{r,c}$ inside each cell; \"none\" leaves cells blank -- the legibility control for large arrays", "default": "full"},
      {"name": "\\showkcl", "type": "bool(0,1)", "meaning": "1 draws a per-column KCL summation node terminating the column rail; 0 omits it (the column rail still connects every cell in the column)", "default": "1"},
      {"name": "\\vlabelprefix", "type": "text", "meaning": "row voltage-source label prefix", "default": "V"},
      {"name": "\\edgelabelstride", "type": "int", "meaning": "show a row/col index label only every Nth line (plus the first and last, always); 1 = label every line -- the legibility control for row/col axis annotation at large nrows/ncols", "default": "1"}
    ],
    "node_naming": "v-source: vin-<row> (generated, index-addressable); crossbar-cell: cell-<row>-<col> (generated, index-addressable); kcl-node: kcl-<col> (generated, index-addressable)",
    "styles": ["cell", "vsource", "kclnode", "awire", "vwire", "axislbl"],
    "operations": [
      {"id": "resize-array", "summary": "edit \\nrows/\\ncols -- the generator re-derives every node name, rail, and KCL node; no coordinate ever needs hand-editing"},
      {"id": "toggle-cell-labels", "summary": "set \\celllabelmode to \"none\" once the array is large enough that per-cell $G_{r,c}$ text would collide (see invariants); the cell's identity is still its node name, not its printed text"},
      {"id": "toggle-kcl", "summary": "set \\showkcl to 0 to omit the per-column summation node while keeping the column rail (e.g. when only the array structure matters, not the readout stage)"},
      {"id": "thin-axis-labels", "summary": "raise \\edgelabelstride so only every Nth row/col gets an index annotation at the array's edge -- the first and last row/col are always labeled regardless of stride"},
      {"id": "add-readout-device", "summary": "as in esl-crossbar-kcl: declare a coordinate below a kcl-<c> node and draw a real circuitikz `to[...]` bipole to it -- this generator does not itself draw readout devices, it only produces the array + rails + KCL nodes for a downstream circuit template (WP-5) to attach them to"},
      {"id": "recolor-role", "summary": "change the domain-semantic color for a whole role by editing that role's \\colorlet binding at the top of the file (inputdomain/weightdomain/analogdomain/digitaldomain) -- never a raw color name inline in a style; keep the esl-crossbar-kcl assignments (weightdomain=green for cell, inputdomain=blue for vsource/vwire, analogdomain=orange for kclnode/awire) unless a family settled-decisions record overrides it"}
    ],
    "invariants": [
      "a template must not name a semantic component the contract's §1 vocabulary does not define (§1 extensibility rule)",
      "style/color bindings carry their contract §2 token role; a role's color is never swapped for decoration",
      "substructure-recognition: recognize known analog SUBSTRUCTURES (current mirror, differential pair, cascode) as the placement UNIT and place them as a block, not transistor-by-transistor; the VMM crossbar-array is this family's concrete ESL substructure. Place blocks first, refine within.",
      "symmetry-axis: matched/paired devices (a differential pair, mirrored current sources, a symmetric crossbar) are placed SYMMETRICALLY across a shared axis (usually vertical).",
      "device-orientation: device ORIENTATION (rotation/flip) is part of placement, not just position — orient devices so pins face their connection and mirror matched devices about the symmetry axis.",
      "power-ground-rails: the power/supply rail is PINNED at the top and the ground rail PINNED at the bottom; rails are placed/connected FIRST (VDD, GND net priority before signal nets).",
      "signal-flow-direction: signal flows LEFT->RIGHT — primary inputs enter at the left, outputs exit at the right; place stages along that axis.",
      "rows-are-inputs: crossbar-cell rows run HORIZONTAL; each row is one activation/input line driven from a v-source at the row's left end (rows left->right, row index increases downward).",
      "cols-are-weights: crossbar-cell columns run VERTICAL; a column is a weight-cell stack sharing one bitline (column index increases left->right). Use crossbar-array for the grid.",
      "current-sums-downward: per-column current flows DOWNWARD and accumulates; each column wire below cell (r,c) carries the running sum I_{1,c}+..+I_{r,c} (label it on the segment).",
      "kcl-at-column-foot: each column terminates in exactly one kcl-node at the column FOOT (below the last cell), tagged \"KCL\"; the column current sums into it.",
      "adc-below-kcl: the adc sits BELOW the kcl-node row (analog current in from the top apex-side, digital word out the far side); never above or beside the summation.",
      "analog-digital-split: the analog/digital split is carried by a domain-band + one dashed domain-boundary; the digital region sits BELOW/AFTER the ADC, the analog accumulation above it.",
      "body-font: Fonts match the chapter body — newtx Times text + math.",
      "show-dont-tell: No equation/annotation callout boxes in the figure; equations live in \\caption.",
      "domain-labels-inside-bands: Domain labels sit INSIDE the shaded bands (small italic, top corner).",
      "comma-subscripts: Two-index subscripts are comma-separated, row then column — G_{i,j}, I_{1,j}.",
      "concrete-indices: Arrays use concrete indices (G_{2,2}), no generic highlighted exemplar cell.",
      "typed-wires: Wire color encodes domain — orange=analog current (awire), gray=digital word (dwire), blue=input voltage (vwire) — the token->role binding is reserved and never swapped across the analog/digital or voltage/current domain boundary (e.g. awire never carries a v-source-to-cell edge, vwire never carries a cell-to-KCL edge). Stealth arrowheads everywhere.",
      "circuitikz-bipole-reservation: circuitikz `to[...]` bipoles are reserved for real EE devices (resistor, current/voltage source, capacitor, etc.); crossbar cells, KCL nodes, and voltage-source glyphs stay the hand-styled figstyle-derived node styles (cell/kclnode/vsource) — never redraw a cell as a circuitikz bipole, and never redraw a real device as a plain hand-styled node. Mixing the two idioms in one figure is intentional and must stay legible (label each readout branch).",
      "circuitikz-bipole-node-identity: every circuitikz `to[...]` bipole carries `name=<id>` (a short, lowercase, dash-free identifier unique within the figure, e.g. `name=rsense`) -- in addition to, never instead of, the bipole-reservation idiom above.",
      "kcl-current-label-clearance: every KCL node has a labeled incoming-current annotation (currentlbl) placed clear of neighboring columns.",
      "palette-additive-not-remapped: the ESL domain-semantic tokens (inputdomain/weightdomain/digitaldomain/ analogdomain) are ADDITIVE to opentikz's five ot* names — never remapped onto or aliased through otblue/otorange/otteal/otpurple/otgray.",
      "kcl-not-sigma-glyph: Column sum is a KCL node (joined lines + a small \"KCL\" tag), not a bare Sigma.",
      "xw-is-digital-logic: x_i . w_{i,j} is a digital multiply (XNOR for +-1, AND for 0/1); the product gates a per-cell current source labeled DAC — signal is analog ONLY after the DAC.",
      "running-column-sums: Column wires carry running sums — below (1,1) is I_{1,1}; below (2,1) is I_{1,1}+I_{2,1}; the KCL yields I_j.",
      "adcbuffer-is-zoomin: adcbuffer is the zoom-in of the Sigma+ADC stage of mixed-signal-mac; keep aligned."
    ]
  }
}
