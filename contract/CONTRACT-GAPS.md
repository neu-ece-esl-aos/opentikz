# Contract Gaps Ledger

Every work package (WP-1 … WP-10, ADR-0005 §D8) appends a row here when it
hits something the frozen backend contract (`backend-contract-v1.2.0.md`)
should have carried but doesn't. This is **not** a place to patch around a
gap — see `CONTRACT.md` for the change-control rule (raise to the contract
owner as a new version + re-notify; never edit the vendored contract copy or
silently couple to TikZ internals to route around a gap).

| WP | What was needed | Why the contract didn't carry it | What was done instead | Status |
|----|------------------|-----------------------------------|------------------------|--------|
| WP-2 | A §1b component name for `esl-architecture-block`'s shared `io` node — the hub a two-tile figure's inter-tile NoC bus edges both attach to, distinct from a `tile`, `pe`, `cpu-core`/`acc-core`, or the bus edge itself. | §1b (Architecture / flow family) names cores, tiles, PEs, mem-block, and the bus edge types (`intra-bus`/`inter-bus`), but no node type for a bare inter-tile junction/hub a bus routes *through* rather than *between named endpoints*. | Modeled `io` as an intent-record entity with a `role` but **no** `component:` field, so the adapter's node_naming derivation and §1-extensibility check simply skip it (not invented as a fake vocabulary id) — see `templates/esl-architecture-block/intent.yaml`. | raised |
| WP-2 | A §1a component name for `esl-crossbar-kcl`'s per-column readout-device branch (a real circuitikz bipole — sense resistor / reference current source — reading a KCL node's summed current). | §1a names conceptual circuit components (`crossbar-cell`, `kcl-node`, `adc`, wire/label types) but no entry for a raw EE device primitive (resistor, current source); it is unclear whether the contract intends these as backend-private (§5, "coordinates ... a backend may implement any way it likes") or as a missing §1 vocabulary gap. | Modeled the readout branch as an intent-record entity with a `role` explaining the ambiguity but **no** `component:` field (same skip-don't-invent treatment as the `io` row above) — see `templates/esl-crossbar-kcl/intent.yaml`. | raised |
| WP-5 | A §1a component name for the `icons/circuits/op-amp` icon — circuitikz's native `op amp` shape (a 3-terminal active device: `+`/`-` inputs, one output), used standalone (no enclosing template/intent record of its own). | Same gap as the WP-2 row above, but a distinct device *class* — an op-amp is an active, multi-terminal device, not a passive bipole (resistor/current-source), so it is not just a bigger instance of the WP-2 row's ambiguity; it is unclear whether §1a's silence covers active devices too. Icons carry no `edit_contract`/intent record (only templates do — `tools/adapter/checks.py` only governs templates), so there is no adapter-checked sidecar to record the skip-don't-invent treatment against for an icon; the icon's own `<name>.meta.json` `description` field states the ambiguity in prose instead (see `icons/circuits/op-amp/op-amp.meta.json`). | Named the icon `op-amp` for discoverability only (catalog `id`/`tags`, not a contract §1 vocabulary claim); drew it with circuitikz's native `op amp` shape, left undyed/neutral like every other real EE device symbol this library draws. No `component:` mapping is claimed anywhere for it. | raised |
| WP-5 | A §1a component name for the capacitor (`to[C]`) readout device in `templates/esl-crossbar-kcl-adc` — broadening circuitikz coverage per ADR-0005 D4 added a third bipole type (capacitor) alongside the resistor/current-source the WP-2 row above already covers. | Same underlying gap as the WP-2 row (no §1a entry for a raw EE device primitive) — recorded as its own row per the coordinator's request to log each distinct device class separately, even though the resolution will likely be the same contract action as the WP-2/op-amp rows above. | Modeled identically to WP-2's precedent: the readout-device-branch intent-record entity carries a `role` but no `component:` field, covering all three device types (R/C/I) under one entity rather than one row per bipole — see `templates/esl-crossbar-kcl-adc/intent.yaml`. | raised |
| WP-7 | The actual per-family `placement_grammar`/`style_decisions`/`semantic_decisions` **content** for each family's §3b settled-decisions record (not just the schema, which §3b already gives inline as a fenced YAML block in the vendored contract). | §3b's own text delegates the worked instances to a companion file — "It is authored as seed content in `settled-decisions-template.md` (one worked default instance per family)" — but WP-1 vendored only `backend-contract-v1.2.0.md` into `contract/` (checksummed, offline, pinned); `settled-decisions-template.md` (and `intent-record-template.md`) were never vendored alongside it, so there is no offline, pinned copy of the content the contract's own §3b text says a backend "MUST honor." | `tools/adapter/settled-decisions/{circuit-schematic,architecture,flow}.yaml` were authored by reading `settled-decisions-template.md` directly from `academic.wfp` (WP-2's own `tools/adapter/settled-decisions/README.md` calls it "starting material for WP-7" and names the live `academic.wfp` path) and transcribing its worked-example content — verbatim for `flow` (~95% textual match), substantially for `circuit-schematic`/`architecture` (folded in with the two cp-4856 fixtures' pre-existing hand-authored invariants). This is not a coupling to the TikZ macro internals the D3 obligation is aimed at (neither of backend #1's two realization files were read) and nothing in this fork depends on `academic.wfp` at build/run time — the transcribed content now lives natively in the three committed YAML files — but it IS a one-time authoring-time dependency on a Phase-1 skill asset other than the vendored contract (ADR-0005 D3's own framing), and it was never recorded here until WP-9 found it. Flagged by `tools/adapter/conformance.py`'s anti-coupling harness (allow-listed as a *named, tracked* exception pending resolution, not silently passed). | raised |

## Column guide

- **WP** — the work package that hit the gap (e.g. `WP-2`, `WP-7`).
- **What was needed** — the concrete thing the build required (a semantic, a
  field, a rule) that has no contract counterpart.
- **Why the contract didn't carry it** — the specific §-reference or omission
  in `backend-contract-v1.2.0.md` that left this undefined.
- **What was done instead** — the interim resolution used to keep the build
  moving (must not be a silent TikZ-internals coupling — name the workaround).
- **Status** — one of: `raised` (sent to contract owner, awaiting a new
  version), `resolved-vN.N.N` (a new contract version closed the gap; note the
  version), `open` (not yet raised).
